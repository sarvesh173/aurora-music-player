PySide6>=6.5.0
mpris_server==0.9.6
mutagen>=1.47.0
# v16.5: yt-dlp + ffmpeg required for Browse full-track downloads
yt-dlp>=2024.0.0
# ffmpeg is a system package — install via: sudo apt install ffmpeg
